Тут должен быть файл с логами, но он сейчас тут не нужен, так как он должен будет создаться автоматически при старте программы
(This is my test work for publishing a repository on GitHub, for this I quickly wrote a mini program in a hurry. There should be a log file here, but it is not needed here right now, since it will have to be created automatically at the start of the program)
